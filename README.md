# Booking Management System

Internal tool for staff to manage customer bookings for available services.

Monorepo structure:

- `backend/` — NestJS REST API (TypeScript, Prisma, PostgreSQL)
- `frontend/` — Next.js App Router application (TypeScript)

See the sections below for full setup instructions.
